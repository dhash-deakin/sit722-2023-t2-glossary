<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .container {
      width: 75%;
      max-width: 100%;
      overflow-x: auto;
    }

    h1 {
      text-align: center;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    th, td {
      padding: 8px;
      text-align: left;
      border: 1px solid #ddd;
    }

    th {
      background-color: #f2f2f2;
    }

    tr:nth-child(even) {
      background-color: #f2f2f2;
    }

    a {
      text-decoration: none;
      color: #007bff;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= title %></h1>
    <p>Welcome to my website showing a collection of DevOps terms and their brief descriptions </p>
    <table>
      <tr>
        <th>ID</th>
        <th>TERM</th>
        <th>DESCRIPTION</th>
        <th>REFERENCES</th>
      </tr>
      <% data.forEach((item, index) => { %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= item.title %></td>
        <td><%= item.description %></td>
        <td><% if (item.reference.startsWith('http')) { %>
          [<%= index + 1 %>] <a href="<%= item.reference %>" target="_blank">Reference</a>
          <% } else { %>
          [<%= index + 1 %>] <%= item.reference %>
          <% } %>
        </td>
      </tr>
      <% }); %>
    </table>
  </div>
</body>
</html>
